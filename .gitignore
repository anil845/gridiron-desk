# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Local config / secrets
.env

# Cached source payloads (Sleeper JSON, nflverse CSVs). Large and re-fetchable;
# never committed.
data/

# Yahoo OAuth token cache (data/ already ignored, belt+suspenders)
data/yahoo_tokens.json

# Podcast caption cache (large, refetchable)
seasons/podcast/vtt/

# Daily projection snapshots (diff source; regenerated each build)
seasons/snapshots/
